eth:
  outputs:
   ci:
      account: AZEIDNH-CZ52343
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: ANKIT970
      warehouse: compute_wh
  target: ci
  