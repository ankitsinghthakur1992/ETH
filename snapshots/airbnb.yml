snapshots:
  - name: airbnb_snap
    relation: source('airbnb', 'apartment_listings')
    config:
      # schema: snaps
      strategy: check
      unique_key: listing_id
      check_cols: 'all'
      # updated_at: updated_at
      # dbt_valid_to_current: "to_date('1970-01-01')"
      # hard_deletes: "new_record"
